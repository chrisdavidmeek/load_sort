import React from "react";

const Home = () => {
  return (
    <div>
      <h1>Shape thing</h1>
      <button>Sort by type</button>
      <button>Sort by color</button>
      <button>Sort by cost</button>
    </div>
  );
};

export default Home;
